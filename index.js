import { initializeApp } from "firebase/app";
import {getDatabase, ref} from "firebase/dataase";

const firebaseConfig = {
   apiKey: 
   authDomain:
   databaseURL:
   projectId:
   sotrageBucket:
   messageSenderId:
   appId:
   measurementId: 
}